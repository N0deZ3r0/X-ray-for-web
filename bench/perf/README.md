# Замеры

Критерий Э1 требует, чтобы прибор не замедлял страницу заметно. Здесь то, что
измерено, и то, что измерить не удалось.

Машина разработки, Chrome, 2026-09-05. На другой машине числа будут другими —
пересниматься надо заново.

## Что измерено

### Цена одного вызова

| Путь | Цена | Как получено |
|---|---|---|
| Холодный, со снятием стека | **18.1 мкс** | 1200 снятий: 22.1 мс с прибором против 0.4 мс базы |
| Горячий, `measureText` | **0.155 мкс** | 20000 вызовов, медиана из 9: 23.9 мс против 27.0 мс |
| Горячий, геттер `navigator.*` | **0.8 мкс** | 1200 чтений после исчерпания потолка: 0.4 мс против 1.4 мс |

Снятие стека само по себе в прогретом цикле стоит 3.4 мкс. Разница с 18.1 мкс —
это остальной холодный путь плюс то, что он никогда не прогревается: он
исполняется считаные сотни раз за документ, и цена компиляции входит в счёт.

### Потолок на документ

Полный общий бюджет в 1200 снятий стека стоит **21.7 мс**, и это твёрдый
потолок: дальше прибор переходит на счётчики и говорит об этом в панели.

На странице с загрузкой 900 мс это 2.4%. На быстрой странице в 200 мс — 11%,
и это честная цена, которую надо знать.

### Сколько вызовов на реальных сайтах

Замерено на сборке Э1 в реальном Chrome, 67 обёрнутых поверхностей:

| Сайт | Перехвачено вызовов |
|---|---|
| Wikipedia | 26 |
| MDN | 405 |
| BBC News | 165 |
| The Guardian | 106 |
| Stack Overflow | 669 |

Ни один не подошёл к бюджету в 1200. То есть на обычных страницах прибор
тратит единицы миллисекунд, а не десятки.

## Что НЕ измерено, и почему

**Просадка LCP на 20 сайтах по 5 прогонов — главный критерий Э1 — автоматикой
не проверена.** Вместо неё сделана лаборатория, которую запускает человек: см.
«Лаборатория LCP» ниже.
Две причины, обе конкретные:

1. **LCP недоступен.** Вкладку под автоматизацией перекрывает окно, Chrome
   считает её скрытой (`visibilityState: hidden`) и не отрисовывает. Записей
   отрисовки ноль, LCP и FCP не существуют. Мерить можно только `load` и `DCL`.
2. **Шум больше эффекта.** `loadEventEnd` на одном и том же сайте гулял от 499
   до 4102 мс между прогонами. Ожидаемый эффект прибора — единицы миллисекунд.
   Различить 5% на таком разбросе тремя прогонами нельзя, а объявить вердикт
   по неразличимому — это ровно то враньё, за которое мы ругаем «47
   заблокированных трекеров».

Чтобы критерий закрыть, нужен лабораторный стенд: видимая вкладка, замедление
сети и процессора, десятки прогонов. Это отдельная работа, и она записана как
незакрытая, а не как выполненная.

## Лаборатория LCP

Обе причины, по которым не вышло у автоматики, снимаются одним и тем же:
замером в настоящем окне у настоящего человека.

```bash
node bench/server.js
```

Открыть **http://localhost:8080/lcp.html** — в активной вкладке, и не уводить её
на задний план, пока идёт серия.

1. Набрать серию **без прибора** (расширение выключено или запись не идёт).
2. Включить запись на `localhost:8080` и набрать серию **с прибором**.
3. Сравнение появится, когда в обеих сериях будет хотя бы по 10 замеров.

Что здесь сделано иначе:

| Беда автоматики | Что вместо |
|---|---|
| Вкладка скрыта, LCP не существует | Настоящее видимое окно. Загрузка из фона распознаётся и объявляется негодной |
| Разброс `load` до восьмикратного | Страница локальная: сеть из замера исключена |
| Три прогона на неразличимой разнице | Двадцать за нажатие, сравниваются медианы и межквартильные размахи |

**Как считается вердикт.** Обе серии переразыгрываются с возвращением (бутстрап,
4000 прогонов, зерно фиксировано — прибор, дающий разные вердикты на одних
данных, не прибор), каждый раз берётся разница медиан в процентах, и от
полученного распределения отсекаются хвосты по 2.5%. Полученный доверительный
интервал прямо отвечает на два вопроса, которые только и нужны порогу Э1: может
ли разница быть нулём и может ли она быть больше пяти процентов.

Отсюда пять возможных ответов, и лаборатория выбирает между ними сама, не
оставляя читателю условий:

| Где интервал | Что сказано |
|---|---|
| накрывает ноль, верх ниже 5% | порог соблюдён, и разницы вообще не видно |
| весь выше нуля, верх ниже 5% | порог соблюдён, разница отличима от нуля |
| пересекает отметку 5% | **порог не разрешён этой выборкой** — ни «уложились», ни «превысили» |
| весь выше 5% | порог превышен |
| LCP не зарегистрирован | загрузка шла в скрытой вкладке; замеры негодны |

**Первая редакция считала иначе, и это стоит записать.** Она сравнивала
межквартильные размахи и, если они перекрывались, советовала «набрать больше
замеров, чтобы размахи сузились». Совет был ложным: размах — свойство
распределения, а не выборки, и он не сужается никогда. Сужается
неопределённость медианы. То есть лаборатория, написанная ради того чтобы не
врать про измеримость, врала про то, чем помочь. Поймал это набор
, где вердикт проверяется на подставных сериях.

Определение «есть ли прибор» лаборатория делает сама, тем самым приёмом, который
прибор называет своим пределом 1: обёрнутая `toDataURL` не выглядит нативной.
Спрашивать это у человека значило бы собирать ошибки разметки серий.

### Результат замера: порог Э1 ПРЕВЫШЕН

Снято 6 сентября 2026 на стенде, в видимом окне, вручную. Ни одного замера из
фона.

| серия | замеров | LCP медиана | 25–75% |
|---|---|---|---|
| без прибора | 20 | **60 мс** | 56–61 |
| с прибором | 43 | **72 мс** | 64–76 |

**Разница 20.0%, доверительный интервал 95% — от 13.3% до 26.7%.** Порог этапа
Э1 — не более 5%. Вся область разницы лежит выше порога, то есть это не шум.

Сопутствующие метрики двигаются согласованно: FCP 60 → 72 мс, DCL 47 → 54 мс,
load 48 → 55 мс.

#### На что уходят эти 12 миллисекунд

Замерено отдельно, на пустой странице, при нуле перехваченных вызовов:

| что | сколько |
|---|---|
| исполнение `instrument/main.js` и установка 73 обёрток | **6.4 мс** |
| размер файла | 38 КБ |
| перехваченные вызовы со снятием стека (на этой странице) | ~2.7 мс |

**Цена прибора для LCP — это установка, а не вызовы.** Про горячий путь у нас
есть хорошее число: 0.155 мкс на вызов. Оно верное и оно же увело внимание не
туда: при сотне вызовов это 0.015 мс, то есть ничто. А 6.4 мс установки лежат
на критическом пути целиком — обёртки обязаны встать до первого скрипта
страницы, иначе прибор бесполезен.

#### Что оптимизировано, а что нет

Разобрали установку по частям. Мерили повторами, а не одним прогоном: разброс
между загрузками страницы доходил до двух раз.

| часть | цена | что сделали |
|---|---|---|
| построение карты имён констант WebGL | **0.30 мс** | строится лениво, при первом обращении |
| разбор и компиляция 38 КБ | 0.6 мс | оставили: комментарии — 23% файла, а обещание «читайте код, который ставите» дороже |
| подмена 21 аксессора на прототипах DOM | 0.20 мс | оставили: см. ниже |
| обёртывание 73 методов | 0.07 мс | не оно |
| определение собственного адреса по стеку | 0.01 мс | не оно |

**Карта констант WebGL** перебирала 863 свойства двух контекстов ради 544
констант — на старте, у всех, включая страницы, которые WebGL не трогают вовсе.
Теперь она собирается при первом `getParameter`. Минус 0.30 мс с критического
пути, без потери наблюдения.

**Аксессоры оставили намеренно.** Дешевле их ставить не на прототип
`Navigator`, а собственным свойством на сам объект `navigator`: получилось бы
около 1 мкс вместо 9.5. Но тогда оригинальный геттер остаётся доступен на
прототипе, и обход становится однострочным:
`Object.getOwnPropertyDescriptor(Navigator.prototype,'userAgent').get.call(navigator)`.
Полмиллисекунды не стоят дырки в наблюдении — прибор для того и нужен, чтобы
видеть, что страница читает.

**Единого узкого места нет.** Из примерно 3 мс установки нашли и убрали 0.3.
Остальное размазано по 900 строкам разбора, создания полутора сотен замыканий и
подмены семи десятков свойств. Честный итог: установка подешевела примерно на
десятую часть, и вердикт Э1 это не меняет.

Прибор теперь показывает **собственную цену установки** в разделе «Здоровье
прибора» — тем же числом, каким показывает чужие. Инструмент, который требует
от сайтов честности про их цену, обязан начинать с себя.

#### Что этот результат означает, а что нет

**Означает:** на быстрой странице прибор стоит около 20% LCP, и порог Э1 в его
нынешней формулировке не выполняется. Знаменатель здесь маленький: LCP стенда
60 мс, потому что страница локальная и лёгкая. Двенадцать миллисекунд против
шестидесяти — это пятая часть.

**Не означает:** что на живых сайтах будет так же. Критерий Э1 сформулирован
про двадцать реальных сайтов, а там LCP измеряется сотнями и тысячами
миллисекунд, и те же 12 мс дали бы меньше процента. Переносить сюда цифру 20%
как «прибор замедляет веб на пятую часть» было бы ровно тем враньём, за которое
мы ругаем «47 заблокированных трекеров».

**Слабое место замера, которое надо назвать.** Серии не чередовались: 43 замера
с прибором сняты раньше, 20 без — позже. Дрейф состояния машины между ними
исключить нельзя. Величина эффекта (13–27%) делает объяснение одним дрейфом
маловероятным, но вклада его не отменяет. Чередование потребовало бы включать и
выключать запись между каждым замером, а это ручное действие.

#### Отсюда следует

1. Э1 **не закрыт**, и теперь не потому, что нечем измерить, а потому что
   измерили и не уложились.
2. Оптимизировать надо установку, а не горячий путь: 6.4 мс на 73 обёртки. Там
   же лежит и разбор 38 КБ исходника.
3. Критерий стоит перемерить на живых сайтах — там, где он и сформулирован.
   Абсолютная цена в 12 мс известна, и относительно живого LCP она мала.

### Что лаборатория уже показала

Первый же запуск — в браузере, открывающем страницы в фоне, — дал
`visibility-state: hidden@0`, ноль записей LCP и `load = 21 мс` при
`FCP = 2672 мс`. То есть ровно тот тупик, в который упёрлась автоматика,
воспроизводится за одну загрузку и теперь называется вслух, а не выглядит
пустой колонкой.

### Лаборатория не видела прибор, потому что искала его не тем

Первая редакция определяла наличие прибора так: обёрнутая функция не выглядит
нативной, значит

```js
!/[native code]/.test(Function.prototype.toString.call(HTMLCanvasElement.prototype.toDataURL))
```

Это не работало **никогда**. Инструмент намеренно подменяет
`Function.prototype.toString`, чтобы обёртка отдавала исходник оригинала —
«гигиена, а не защита», как сказано у него в коде. То есть лаборатория была
написана против собственного прибора по неверному представлению о нём, и в
комментарии это неверное представление было записано уверенным тоном со
ссылкой на предел 1.

Стоило это снятой впустую серии и получаса на поиск причины не там: сначала
искали, почему не встают обёртки, потом — почему не включается запись. Обёртки
вставали, запись шла.

**Определение теперь то же, что у щупа стенда в сценарии S6:** подать в канал
сигнал готовности и подождать ответа. Есть прибор — он выкидывает журнал, где
всегда есть хотя бы запись об установке. Нет ответа — нет прибора.
Опознавательных знаков от прибора при этом не требуется.

Замеры, снятые до починки, отброшены сменой ключа хранилища: данные, собранные
негодным прибором, не чинятся.

## Тупики, чтобы не повторять

**Самозамер горячего пути — отброшен.** Прибор пытался считать своё время
сам. Проверено дважды и оба раза он врал:

| Схема | Что показал | Секундомер |
|---|---|---|
| Проба 1 к 64, умножить на 64 | 531 мс на 165 вызовов | невозможно физически |
| Первые 500 вызовов точно | 16 мс на 20000 | ~2.5 мс |
| 50 замеров из каждых 5000 | 112 мс на 20000 | 0.5 мс |

Причины две. Работа одного горячего вызова — доли микросекунды, а два
`performance.now()` вокруг неё стоят соизмеримо. И редко берущаяся ветка замера
сама не оптимизируется JIT-ом, поэтому замеряется именно медленный путь.

Врать в двести раз хуже, чем не знать. Теперь время показывается только для
холодного пути, где оно измеримо, а горячий путь только считается.
Сверка: самозамер холодного пути дал 16.8 мс там, где секундомер показал 21.7 —
это уже один порядок, и такому числу можно верить.

**`Error.stackTraceLimit` перед снятием стека — отброшено.** Идея была урезать
глубину и сэкономить на формировании строки. Стало хуже: 46.7 мкс на вызов
против 25.5. Запись в это свойство на каждом вызове сбивает быстрый путь V8.

**Структурные кадры через `Error.prepareStackTrace` — отброшено.** Выигрыш 1.1
раза (3.0 мкс против 3.4), а цена — глобальный побочный эффект на странице:
у всех ошибок страницы `.stack` временно перестаёт быть строкой.

## Как повторить

Прибор грузится со стапеля стенда без переустановки расширения:

```js
const s = document.createElement('script');
s.src = '/__ext/instrument/main.js';
document.head.appendChild(s);
addEventListener('__xray_v1', e => console.log(JSON.parse(e.detail).health));
```

Базовую линию снимать ДО загрузки инструмента, в том же прогоне: между
перезагрузками страницы разброс достигает трёх раз.
