# Мусор операционных систем и облачных папок
.DS_Store
Thumbs.db
desktop.ini
*~

# Отчёты, сохранённые прибором. Они содержат данные с реальных сайтов, где вы
# были, — в репозиторий им нельзя даже с включённой редакцией.
рентген-*.json
рентген-*.txt
рентген-*.md
xray-report-*

# Зависимостей у проекта нет и не планируется, но если кто-то поставит их
# локально — пусть не уедут в историю.
node_modules/
package-lock.json

# Локальные настройки редакторов
.vscode/
.idea/

# Настройки локального агента, а не проекта — рядом с .vscode и .idea.
.claude/
