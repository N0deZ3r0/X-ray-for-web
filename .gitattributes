# Переводы строк в репозитории всегда LF, независимо от системы автора.
# Иначе один и тот же файл, открытый на Windows и на Linux, даёт «изменён
# целиком» — а в проекте, где важно, что именно правилось, это шум.
* text=auto eol=lf

# Значки — двоичные, их трогать нельзя.
*.png binary
